Options -Indexes
RewriteEngine On

# Security headers
Header set X-Content-Type-Options "nosniff"
Header set X-Frame-Options "SAMEORIGIN"
Header set X-XSS-Protection "1; mode=block"

# Prevent direct access to .php files in subdirectories
RewriteRule ^(?!index\.php|assets/) - [F,L]
